using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlowerTActions : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("T: I am alive, ready to burn enemies");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
